{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "uv run alembic upgrade head && uv run aptagent run",
    "restartPolicyType": "NEVER"
  }
}
